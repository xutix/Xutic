# SOZO Dock 系统拓扑图

本文档用来解释 SOZO Dock 从用户操作到硬件执行的整体运行逻辑。  
图使用 Mermaid 编写，GitHub 可以直接渲染；本地 VS Code 如果想预览，可以安装 Mermaid Preview 类插件。

## 1. 总体系统拓扑

SOZO Dock 的核心不是某一个 App，也不是某一个灯效，而是一套“硬件能力 + 固件服务 + 协议 + App 体验”的系统。

```mermaid
%%{init: {"theme": "base", "themeVariables": {"background": "#ffffff", "primaryColor": "#f7fbff", "primaryTextColor": "#102033", "primaryBorderColor": "#9cc9e8", "lineColor": "#4f6b82", "secondaryColor": "#eef7ff", "tertiaryColor": "#f8fafc", "fontFamily": "Microsoft YaHei, Arial"}}}%%
flowchart TB
    user[用户] --> appLayer[桌面软件 / 串口工具 / 蓝牙]

    subgraph apps[上位机与用户入口]
        pyside[PySide6 专业调试版 App]
        electron[Electron 正式版 App]
        storybook[Storybook 组件库]
        serialTool[串口调试工具]
    end

    appLayer --> pyside
    appLayer --> electron
    appLayer --> serialTool

    pyside --> protocol[统一串口协议<br/>UTF-8 line protocol / 115200]
    electron --> protocol
    serialTool --> protocol

    subgraph firmware[ESP32 固件底座]
        router[CommandRouter<br/>命令路由]
        config[ConfigManager<br/>配置保存]
        ledLayout[LedLayout<br/>灯带布局映射]
        ledEngine[LedEngine<br/>WS2812 输出]
        ledEffects[LedStripController<br/>灯效渲染]
        audioFrame[AudioReactiveFrame<br/>统一音频帧]
        mic[Inmp441Input<br/>设备麦克风处理]
        tap[TapDetector<br/>敲击识别]
        action[ActionController<br/>动作执行]
        ble[BLE HID<br/>媒体键 / 键盘动作]
    end

    protocol --> router
    router --> config
    router --> ledEffects
    router --> tap
    router --> action

    config --> ledLayout
    ledLayout --> ledEngine
    ledEffects --> ledEngine

    mic --> audioFrame
    protocol --> audioFrame
    audioFrame --> ledEffects

    tap --> action
    action --> ble
    action --> ledEffects

    subgraph hardware[硬件层]
        leds[三段 WS2812 / SK6812 灯带]
        inmp[INMP441 麦克风]
        imu[LSM6DS3 / IMU]
        usb[USB 串口]
        hid[蓝牙 HID]
    end

    ledEngine --> leds
    inmp --> mic
    imu --> tap
    usb --> protocol
    ble --> hid
```

## 2. 产品分层逻辑

后续开发时，优先看这张图判断“这次改动应该落在哪一层”。

```mermaid
%%{init: {"theme": "base", "themeVariables": {"background": "#ffffff", "primaryColor": "#f7fbff", "primaryTextColor": "#102033", "primaryBorderColor": "#9cc9e8", "lineColor": "#4f6b82", "secondaryColor": "#eef7ff", "tertiaryColor": "#f8fafc", "fontFamily": "Microsoft YaHei, Arial"}}}%%
flowchart TB
    subgraph experience[用户体验层]
        overview[总览]
        lightPage[灯效]
        audioPage[拾音]
        knockPage[敲击交互]
        devicePage[设备 / 调试]
    end

    subgraph appLogic[App 逻辑层]
        state[AppState / Store<br/>界面状态]
        contract[Device Contract<br/>能力真假来源]
        commands[Device Commands<br/>命令构造]
        parser[Protocol Parser<br/>状态解析]
        pcAudio[PC Audio Processor<br/>电脑音频特征提取]
    end

    subgraph protocolLayer[协议层]
        status[STATUS / KNOCK_STATUS / PLOT_STATUS]
        writeCmd[MODE / COLOR / AUDIO_SOURCE / AUDIO_FRAME / SAVE]
    end

    subgraph deviceCore[设备核心层]
        cfg[ConfigManager]
        lights[灯带底座]
        audio[音频底座]
        knock[敲击底座]
        actions[动作底座]
    end

    experience --> state
    state --> commands
    contract --> experience
    parser --> state
    pcAudio --> writeCmd

    commands --> writeCmd
    status --> parser
    writeCmd --> deviceCore
    deviceCore --> status

    cfg --> lights
    cfg --> audio
    cfg --> knock
    knock --> actions
    audio --> lights
```

### 分层原则

- UI 只负责展示和交互，不直接拼底层串口命令。
- 命令统一从 `device_commands.py` 或后续 Electron 的 command builder 出。
- 固件参数以 `ConfigManager` 为保存中心。
- 灯效层只消费统一音频帧，不直接关心 MIC / PC 来源。
- 敲击识别只输出事件，动作执行由动作层负责。

## 3. 音频链路拓扑

这是当前最容易混乱的一条链路。原则是：MIC 和 PC 可以在前面各自处理，但最后必须统一成 `AudioReactiveFrame`。

```mermaid
%%{init: {"theme": "base", "themeVariables": {"background": "#ffffff", "primaryColor": "#f7fbff", "primaryTextColor": "#102033", "primaryBorderColor": "#9cc9e8", "lineColor": "#4f6b82", "secondaryColor": "#eef7ff", "tertiaryColor": "#f8fafc", "fontFamily": "Microsoft YaHei, Arial"}}}%%
flowchart LR
    subgraph micPath[MIC 音频路径]
        micPcm[INMP441 PCM<br/>固件采集]
        micProc[Inmp441Input<br/>RMS / 包络 / 鼓点]
        micFrame[MIC AudioReactiveFrame]
    end

    subgraph pcPath[PC 音频路径]
        pcPcm[电脑系统音频 PCM<br/>WASAPI loopback]
        norm[NormalizedAudioBlock<br/>mono / 归一化 / RMS]
        pcProc[PcAudioFeatureExtractor<br/>dB 映射 / 自动电平 / 频段 / 鼓点]
        serialFrame[AUDIO_FRAME<br/>energy,bass,mid,treble,beat]
        pcFrame[PC AudioReactiveFrame]
    end

    subgraph ledPath[灯效路径]
        select[音源选择<br/>MIC 或 PC]
        frame[统一 AudioReactiveFrame<br/>level,bass,mid,treble,beat]
        effects[AudioPulse / FlameAudio / BassRipple]
        output[WS2812 输出]
    end

    micPcm --> micProc --> micFrame --> select
    pcPcm --> norm --> pcProc --> serialFrame --> pcFrame --> select
    select --> frame --> effects --> output
```

### 音频链路规则

- PC 模式下，不叠加 MIC。
- MIC 模式下，不读取 PC 音频帧。
- PC 音频调参只改 App 端 `PcAudioFeatureExtractor`。
- MIC 音频调参只改固件端 `Inmp441Input`。
- 灯效调参只改灯效渲染，不拿灯效参数补偿音源差异。

## 4. 灯带系统拓扑

灯带系统分为“物理布局”和“灯效渲染”两件事。灯效不应该自己处理每条灯带的方向和数量。

```mermaid
%%{init: {"theme": "base", "themeVariables": {"background": "#ffffff", "primaryColor": "#f7fbff", "primaryTextColor": "#102033", "primaryBorderColor": "#9cc9e8", "lineColor": "#4f6b82", "secondaryColor": "#eef7ff", "tertiaryColor": "#f8fafc", "fontFamily": "Microsoft YaHei, Arial"}}}%%
flowchart TB
    hw[HardwareConfig<br/>默认 GPIO / 默认数量 / 默认方向]
    cfg[ConfigManager<br/>保存用户或产品配置]
    layoutCfg[LedLayoutConfig<br/>三条灯带配置]
    layout[LedLayout<br/>逻辑坐标到物理灯珠映射]
    engine[LedEngine<br/>FastLED buffer / show / clear]
    effect[LedStripController<br/>模式 / 颜色 / 动画]
    strip[真实灯带<br/>Center / Left / Right]

    hw --> layoutCfg
    cfg --> layoutCfg
    layoutCfg --> layout
    layout --> engine
    effect --> engine
    engine --> strip
```

### 灯带规则

- 产品默认数量写在 `HardwareConfig`。
- 可变产品配置由 `ConfigManager` 保存。
- `LedLayout` 负责方向、数量、逻辑坐标。
- `LedEngine` 负责真实 WS2812 输出。
- 灯效只用逻辑接口，不直接关心某条灯带是否反向。

## 5. 敲击交互拓扑

敲击系统分三层：传感器、识别、动作。不要把动作逻辑塞进传感器读取里。

```mermaid
%%{init: {"theme": "base", "themeVariables": {"background": "#ffffff", "primaryColor": "#f7fbff", "primaryTextColor": "#102033", "primaryBorderColor": "#9cc9e8", "lineColor": "#4f6b82", "secondaryColor": "#eef7ff", "tertiaryColor": "#f8fafc", "fontFamily": "Microsoft YaHei, Arial"}}}%%
flowchart LR
    imu[IMU 原始数据<br/>单 IMU 或双 IMU]
    tap[TapDetector<br/>峰值 / 面积 / 双击 / 三击]
    mode[交互模式<br/>左右识别 / 仅双击]
    event[Knock Event<br/>LEFT / RIGHT / UNKNOWN / DOUBLE / TRIPLE]
    action[ActionController<br/>动作绑定]
    hid[BLE HID / USB 后续]
    ledFeedback[角落闪灯 / 灯效切换]

    imu --> tap --> mode --> event --> action
    action --> hid
    action --> ledFeedback
```

### 敲击规则

- 单 IMU 是 V1 小批量的优先硬件策略。
- 双 IMU 可以提供左右识别，但用户仍应能关闭左右识别，改用“仅双击”。
- 调试参数可以保留在 PySide6 专业版。
- 正式用户界面优先给“低 / 默认 / 高”三档灵敏度，不暴露大量专家参数。

## 6. App 角色边界

```mermaid
%%{init: {"theme": "base", "themeVariables": {"background": "#ffffff", "primaryColor": "#f7fbff", "primaryTextColor": "#102033", "primaryBorderColor": "#9cc9e8", "lineColor": "#4f6b82", "secondaryColor": "#eef7ff", "tertiaryColor": "#f8fafc", "fontFamily": "Microsoft YaHei, Arial"}}}%%
flowchart TB
    subgraph pyside[PySide6 专业版]
        p1[硬件验证]
        p2[协议验证]
        p3[专家参数调试]
        p4[校准和采样]
    end

    subgraph electron[Electron 正式版]
        e1[普通用户控制]
        e2[好看的产品 UI]
        e3[少量安全参数]
        e4[未来跨平台]
    end

    subgraph storybook[Storybook]
        s1[组件外观调试]
        s2[Design System]
        s3[不接真实硬件]
    end

    subgraph firmware[固件]
        f1[真实硬件能力]
        f2[参数保存]
        f3[灯效 / 音频 / 敲击 / HID]
    end

    pyside --> firmware
    electron --> firmware
    storybook -.只看 UI.-> electron
```

### App 规则

- PySide6 是专业工具，不等于最终消费级 App。
- Electron 是未来正式 App，但必须按 `device_contract_v1.json` 做真实能力判断。
- Storybook 只调 UI，不连接串口、不接音频、不碰固件。
- 如果某个功能固件不支持，App 不应该做成可点击的真实按钮。

## 7. 当前不属于产品运行链路的东西

这些文件或目录目前属于研发辅助，不是产品底座：

```text
captures/
tools/analyze_led_video.py
tools/capture_audio_compare.py
tools/capture_pc_audio_profile.py
```

用途：

- 对比 MIC / PC 音频采样。
- 分析灯带视频变化。
- 辅助调参。

发布产品或打包 App 时，不应该把采样数据一起带进去。

## 8. 后续开发时怎么用这张图

新增功能前先判断它属于哪一层：

```text
改 UI 观感        → App / Storybook / Design System
改参数同步        → Protocol / Device Commands / ConfigManager
改音频灵动感      → MIC Processor 或 PC Audio Processor
改灯效好不好看    → LedStripController 或后续 Effect 模块
改灯带数量方向    → HardwareConfig / ConfigManager / LedLayout
改敲击准确率      → TapDetector
改双击触发动作    → ActionController
```

如果一次改动跨了三层以上，默认应该拆小步做。
